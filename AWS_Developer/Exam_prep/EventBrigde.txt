Minden amit lelogol a CloudTrail, az az EventBridge-en is átmegy.
	Az eventekre filtereket építhetünk, amiket rule-oknak hívnak. Ha a filter pattern-e matchel, akkor létrejön az alert, amit tovább lehet küldeni más service-eknek, pl.: SNS.
		Pl.: Meghívódik az IAM service AssumeRole endpointja, amivel egy Role-t delegálunk egy usernek, ebból log születik a CloudTrailen, ami átmegy az EventBridge-en, aminek van erre egy alartje,
		 és SNS fan-outolja a SES-nek (Simple Email-service)
		 Vagy megváltozik egy SG indounRule-ja.


Parts of it:

	The Default Event Bus

	Custom Event Bus	

	Partner Event Bus capability: Külső progik is küldhetnek be: DataDog, AWS SaaS Partners

Formerly: CloudWatch Events,



	Chron
		:Every hour run a Lambda
		
	React to event patterns:
		An IAM user signs in the console ->SNS Topic Email notification
		Ar listen to any API request (CloudTrail)

	Archive Events
	
	Events are in JSON format
	
	//Custom Event Pattern builder/tester is a handy function.
	
	
	Accuntok közt is lehet küldeni megfelelő policy-vel































